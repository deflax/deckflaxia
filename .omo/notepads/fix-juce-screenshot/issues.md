# Issues: fix-juce-screenshot

- Task 2 local verification limitation: `ctest --test-dir build-juce -R "JuceUi.Screenshot" --output-on-failure` and direct `cmake -P tests/ui/VerifyJuceScreenshot.cmake` script-surface checks could not run in this environment because `ctest`/`cmake` are not installed; source-level checks confirmed the CTest registration now executes `tests/ui/VerifyJuceScreenshot.cmake` and the script contains marker, size, file, PNG signature/IHDR, and `1920x1080` dimension assertions.

- Task 3 blocked/failing verification: `cmake --build build-juce -j6` could not run because `/usr/bin/bash: line 1: cmake: command not found`, and targeted `ctest --test-dir build-juce -R "JuceUi.Screenshot|JuceUi.ComponentTree|PlayableSmoke.AppSmoke" --output-on-failure` could not run because `/usr/bin/bash: line 1: ctest: command not found`; see `.omo/evidence/real-playable-juce/task-fix-juce-screenshot-ctest.log`. The direct existing `build-juce/Deckflaxia_artefacts/Debug/Deckflaxia` screenshot command ran and wrote a PNG, but its log did not contain the required source/size markers and machine PNG parsing found `1280x720`, not `1920x1080`; see `.omo/evidence/real-playable-juce/task-fix-juce-screenshot.log`.
- Final wave: F1 Plan Compliance and F3 Runtime Screenshot QA are blocked until a local environment rebuilds with `cmake`, runs targeted `ctest`, and regenerates screenshot evidence from the rebuilt binary. F2 Code Quality approved. F4 Scope Fidelity approved after correcting baseline context that layout/sizing changes belonged to the prior `gui-layout-improvement` plan.
