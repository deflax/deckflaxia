# GitHub Actions Disabled

GitHub Actions CI is intentionally disabled for this repository.

Do not add `.yml` or `.yaml` workflow files here, restore workflow triggers, or move disabled workflow artifacts back into this directory unless the user explicitly asks to re-enable CI. Future feature plans should use the local verification commands documented in `docs/user-runbook-developer-operations.md` instead.
